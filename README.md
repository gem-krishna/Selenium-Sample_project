## Prompt
Use the migration instructions in .github/copilot/copilot_instructions.md to convert all Selenium + Java and RestAssured step definitions from /src/test/java/stepDefinitions/LoginSteps.java into Playwright Java. Generate the factory, page objects, Playwright steps, and remove old dependencies. Validate using Scan-SeleniumUsage.ps1 and MigrationReport.ps1.
